/**
 * Avalanche simulation engine — pure JS, no side effects.
 * @param {Object[]} debts
 * @param {Object|null} loanConfig
 * @returns {{ timeline: Object[], totalInterest: number, months: number }}
 */
export function simulate(debts, loanConfig = null) {
  // TODO: implement avalanche simulation
  return { timeline: [], totalInterest: 0, months: 0 }
}
